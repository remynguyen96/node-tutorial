<div class="row">
    <div class="col-md-8 col-md-offset-2">
        <h1>User Profile</h1>
        <hr>
        <h2>My Orders</h2>
        {{# each orders }}
            <div class="panel panel-info">
                <div class="panel-body">
                    <ul class="list-group">
                        {{# each this.items }}
                            <li class="list-group-item">
                                <span class="badge">{{ this.price }}</span>
                                {{ this.item.title }} | {{ this.qty }} Units
                            </li>
                        {{/each}}
                    </ul>
                </div>
                <div class="panel-footer">
                  <p><strong>Total Price: ${{ this.cart.totalPrice }} -- Total Price: {{ this.cart.totalQty }}</strong></p>
                  <p><strong>Name: {{ this.name }}</strong></p>
                  <p><strong>Address: {{ this.address }}</strong></p>
                  <p><strong>PaymentID: {{ this.paymentId }}</strong></p>
                </div>
            </div>
        {{/each}}
    </div>
</div>
